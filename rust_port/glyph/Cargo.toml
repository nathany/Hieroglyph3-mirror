[package]
name = "glyph"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Shared support code for the Rust ports of the Hieroglyph3 book samples"

[dependencies]
windows.workspace = true
