[package]
name = "image_processor"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Rust port of the ImageProcessor sample (Applications/ImageProcessor) - no text overlay"

[dependencies]
glyph.workspace = true
windows.workspace = true
glam.workspace = true
