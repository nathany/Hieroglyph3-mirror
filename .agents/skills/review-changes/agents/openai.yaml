interface:
  display_name: "Review Changes"
  short_description: "Review diffs and audit Odin/C++ reference fidelity"
  default_prompt: "Use $review-changes to review the requested diff or audit the current Odin port against its C++ reference."
